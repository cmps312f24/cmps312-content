/* import 'package:flutter/material.dart';

abstract class Screen {
  final String route;
  final String title;
  final IconData? icon;

  const Screen(this.route, this.title, [this.icon]);
}

class Hello extends Screen {
  Hello() : super('hello', 'Hello (Flutter Basics)', Icons.home);
}

class FlutterLogo extends Screen {
  FlutterLogo() : super('flutter-logo', 'Flutter Logo (Card)');
}

class ClicksCounter extends Screen {
  ClicksCounter() : super('clicks-counter', 'Clicks Counter (Manage State)');
}

class Welcome extends Screen {
  Welcome() : super('welcome', 'Welcome (Manage State)');
}

class DividerScreen extends Screen {
  DividerScreen() : super('divider', 'Divider');
}
 */